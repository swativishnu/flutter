import 'package:flutter/cupertino.dart';

class MyCustomWidget1 extends StatelessWidget {
  const MyCustomWidget1({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Custom widget 1'),
    );
  }
}
